---
description: Figma MCP 讀取規則模板
globs:
alwaysApply: true
---

# Figma MCP 讀取規則

## 概述

本文件定義了透過分析 Prompt 或藉由 Figma MCP (Model Context Protocol) 讀取 Figma 設計稿時必須遵循的標準規則和格式，確保每次讀取都能獲得一致且完整的資訊。

## 標準 Prompt 格式

當需要查詢 Figma flow 時，應該使用以下標準 prompt：

```
請查看這個 Figma 連結，分析並提供：

1. **起始 frame 及其所有相關互動 frame**
2. **每個 frame 的完整連結**
3. **frame 之間的邏輯關係和互動順序**

連結：[Figma prototype URL]或是[Figma selection URL]

請確保包含：
- 主頁面 frame
- 所有彈出的對話框/選單 frame
- 任何在該 flow 中可觸發的互動元素對應的 frame
- 每個 frame 的 node-id 和完整原型連結
```

## 分析要求

### 必須包含的資訊

1. **Frame ID** - 例如：`1:413`
2. **Frame Name** - 例如：`[專案特定的命名格式]`
3. **完整連結** - 保持原始 URL 的 `t` 參數值
4. **互動關係** - 說明 frame 之間的觸發關係

### 分析方法

1. **系統性分析** Figma 數據中的所有 frame
2. **識別互動關係** - 哪些元素會觸發哪些 frame
3. **追蹤完整流程** - 從起始 frame 開始的所有可能路徑
4. **使用提供的 URL 參數** - 保持一致的 `t` 參數值

## 標準回應格式

每個分析的 frame 都應該按照以下格式提供：

### Frame 資訊格式範例

```markdown
### [序號]. [Frame 功能描述]

- **Frame ID**: `[node-id]`
- **Frame Name**: `[frame-name]`
- **連結**: `[完整的 figma prototype URL]`
```

## 注意事項

### 範圍控制

1. **範圍限制** - 只提供指定 flow 範圍內的 frame
2. **相關性檢查** - 確保所有 frame 都與目標功能相關
3. **完整性保證** - 不遺漏任何重要的互動 frame
4. **邏輯順序** - 按照使用者操作流程排列 frame

### URL 處理

1. **參數一致性** - 保持原始 URL 的所有參數
2. **連結有效性** - 確保提供的連結可以直接訪問

### 資料品質

1. **命名準確性** - Frame 名稱應該反映實際功能
2. **ID 正確性** - Frame ID 必須與 Figma 中的實際 ID 一致
3. **描述清晰性** - 功能描述應該清楚易懂

## 常見錯誤避免

### ❌ 錯誤做法

- 包含不相關的 frame（如其他功能模組的測試頁面）
- 改變原始 URL 的參數值
- 遺漏重要的互動 frame（如確認對話框、錯誤狀態等）
- 提供不完整的 frame 資訊
- 忽略 frame 之間的邏輯關係

### ✅ 正確做法

- 只列出起始 frame 及其直接相關的互動 frame
- 保持 URL 參數的一致性
- 提供完整的互動流程分析
- 包含所有必要的狀態 frame
- 清楚說明 frame 之間的觸發關係

## 工具使用說明

### Figma MCP 呼叫方式

1. **獲取 Figma 資料**：使用 `mcp_figmaContext_get_figma_data`
2. **下載圖片素材**：使用 `mcp_figmaContext_download_figma_images`
3. **分析原型連結**：解析 prototype URL 中的參數

### 參數解析

從 Figma prototype URL 中解析重要參數：

- `node-id`: Frame 的節點 ID
- `t`: 版本/時間戳參數（必須保持）
- `scaling`: 縮放設定
- `starting-point-node-id`: 起始節點 ID

## 品質保證

### 檢查清單

完成 Figma 分析後，使用以下清單進行品質檢查：

- [ ] 所有 frame 都有完整的 ID、名稱、連結
- [ ] 連結可以正常訪問
- [ ] 互動關係描述清楚
- [ ] 沒有遺漏重要的狀態頁面
- [ ] 範圍控制適當，沒有包含無關內容
- [ ] 格式符合標準規範

### 驗證方法

1. **連結測試**：逐一點擊提供的 Figma 連結確認可訪問
2. **完整性檢查**：對照 Figma 原型確認沒有遺漏 frame
3. **邏輯驗證**：確認互動流程符合使用者操作邏輯
4. **格式檢查**：確保輸出格式符合標準規範
