---
description: 樣式系統規範模板
globs:
alwaysApply: false
---

# 樣式系統

本應用程序使用 [CSS框架] 進行樣式設置。

## 主要樣式文件

- `[全域樣式文件]` - 全局 CSS 樣式
- `[樣式配置文件]` - [CSS框架] 配置

## 樣式模式

- 使用 `[樣式屬性]` 屬性應用 [CSS框架] 類
- 使用 `[樣式工具1]` 和 `[樣式工具2]` 進行條件式類名組合
- 使用 `[配置文件]` 進行 UI 組件樣式配置

## 組件樣式

UI 組件樣式基於 [UI框架]，位於 `[組件樣式目錄]` 目錄。

## 主題系統（如適用）

- **主題配置**: [主題配置說明]
- **深色模式**: [深色模式支援說明]
- **主題切換**: [主題切換機制說明]

## 響應式設計

- **斷點設定**: [響應式斷點說明]
- **行動裝置優先**: [行動裝置優先策略說明]
- **適配策略**: [不同螢幕尺寸適配說明]

## 樣式組織

### 樣式檔案結構

```
[樣式目錄]/
├── [全域樣式]              # 全域樣式定義
├── [組件樣式目錄]/         # 組件專用樣式
├── [工具樣式目錄]/         # 工具類樣式
└── [主題樣式目錄]/         # 主題相關樣式
```

### 命名慣例

- **類名命名**: 使用 [命名格式] 格式
- **變數命名**: 使用 [變數命名格式] 格式
- **組件樣式**: 遵循 [組件樣式命名規則]

## 樣式最佳實踐

1. **一致性**: 保持整個應用程序的視覺一致性
2. **可維護性**: 使用模組化的樣式結構
3. **效能**: 避免不必要的樣式重複和衝突
4. **可讀性**: 使用語意化的類名和註解

## 自定義樣式

### 擴展基礎樣式

```[樣式語言]
/* 自定義樣式範例 */
.[自定義類名] {
  [樣式屬性]: [樣式值];
}
```

### 組件專用樣式

- 組件專用樣式應放置在組件同一目錄下
- 使用 [樣式模組化方案] 避免樣式衝突
- 遵循 [樣式作用域規則]
